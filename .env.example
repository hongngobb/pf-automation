# Environment Configuration
ENVIRONMENT=RC
BASE_URL=https://hongngotest1.myshopify.com/admin/apps/rc-pagefly-1

# Authentication Settings
STORAGE_STATE_PATH=./auth-state.json

# PageFly Configuration
APP_IFRAME=app-iframe
APP=RC
HELPER_SCRIPTS_PATTERN=https://cdn\.shopify\.com/extensions/.*/pagefly-rc-\\d+/assets/pagefly(-helper)?\.js
EDITOR_IFRAME=//iframe[starts-with(@name, 'frame://RC/modal/') and contains(@name, '/src')]
OVERLAY_MODAL_IFRAME=//iframe[starts-with(@name, 'frame://RC/modal/') and not(contains(@name, '/src'))]
DRAG_DROP_IFRAME=pf-sandbox

# Store Configuration
STORE=hongngotest1
PASSWORD=1

# Browser Configuration
BROWSER=chromium
HEADLESS=false
USER_DATA_DIR=./user-data

# Test Configuration
WAIT_DEFAULT=5000
WAIT_IMPLICIT=20000
WAIT_EXPLICIT=15000
WAIT_PAGE_LOADED=60000
ACTIVE_PAGE_LOADED=true
WAIT_SLEEP_STEP=0

# Screenshot Configuration
SCREENSHOT_PASSED_TCS=true
SCREENSHOT_FAILED_TCS=true
SCREENSHOT_SKIPPED_TCS=false
SCREENSHOT_ALL_STEPS=false

# Video Recording
VIDEO_RECORD=false

# Retry Configuration
RETRY_TEST_FAIL=2

# Report Configuration
AUTHOR=PageFly Team
PROJECT_NAME=PageFly Automation Test
REPORT_TITLE=PageFly Automation Test Report
